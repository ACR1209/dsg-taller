/**
 * Dada una array ordenada de enteros distintos y un valor objetivo,
 * devuelve el índice si se encuentra el objetivo.
 * 
 * Si no, devuelve el índice donde estaría si se insertara ordenado.
 * 
 * Ex:
 * 1) 
 * Input: nums = [1,3,5,6], target = 5
 * Output: 2
 * 2) 
 * Input: nums = [1,3,5,6], target = 2
 * Output: 1
 */

export function searchInsertPosition(nums, target) {
    
}