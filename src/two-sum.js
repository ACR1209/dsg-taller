/**
 *   Dado un array de enteros nums y un entero target, devuelva los índices de 
 *   los dos números tales que sumen target.
 * 
 *   Puede suponer que cada entrada tendría exactamente una solución, y 
 *   no puede utilizar el mismo elemento dos veces.
 * 
 *   Puede devolver la respuesta en cualquier orden.
 * 
 *   Ex:
 *   1)
 *   Input: nums = [2,7,11,15], target = 9
 *   Output: [0,1]
 *   
 *   2)
 *   Input: nums = [3,2,4], target = 6
 *   Output: [1,2]
 */


export function twoSum(nums, target) {
    
}